import { Typography, Container } from '@material-ui/core'
import React from 'react'

export const Master = ({ id }: { id: number }) => {
  return (
    <Container>
      <Typography>Hello, Master!</Typography>
    </Container>
  )
}
