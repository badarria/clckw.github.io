import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles({
  root: {
    margin: '16px',
    maxWidth: '140px',
    minWidth: '100px',
  },
  input: { fontSize: '14px' },
})
