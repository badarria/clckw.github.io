import { makeStyles } from '@material-ui/core/styles'
export const useStyles = makeStyles({
    btn: { margin: '30px 0 0' },
    card: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
    },
    title: { padding: '16px 0 24px' },
})
